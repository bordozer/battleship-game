spring:
  application:
    name: battleship-game-server
  profiles:
    active: dev
#  mvc:
#    view:
#      prefix: /WEB-INF/jsp/
#      suffix: .jsp

server:
  port: 8036
  servlet:
    session:
      timeout: 864000

logging:
  level:
    root: info

---
spring:
  profiles: dev
